# requirements.txt tối giản và ổn định cho Streamlit Cloud

# Ứng dụng chính và API
streamlit==1.50.0  # Hoặc bỏ phiên bản để dùng bản mới nhất: streamlit
requests==2.32.5
pillow==10.4.0

# Trích xuất từ khóa
yake==0.4.8

# OCR (Quan trọng: Dùng phiên bản CPU cho môi trường Cloud mặc định)
# PaddleOCR thường cài đặt paddlepaddle CPU/GPU tùy thuộc vào môi trường.
# Nếu bạn đã cài GPU cục bộ, hãy thay thế bằng phiên bản CPU.

# Nếu bạn muốn đảm bảo là phiên bản CPU, hãy thử cài riêng:
paddlepaddle==2.5.2 # Dùng phiên bản CPU thay vì -gpu
paddleocr==2.6.1.3
